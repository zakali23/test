{% extends "admin/base_admin.html.twig"%}

{% block body %}
    <h1>Syndic</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ syndic.id }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ syndic.nom }}</td>
        </tr>
        <tr>
            <th>Adresse</th>
            <td>{{ syndic.adresse }}</td>
        </tr>
        <tr>
            <th>Codepostal</th>
            <td>{{ syndic.codePostal }}</td>
        </tr>
        <tr>
            <th>Ville</th>
            <td>{{ syndic.ville }}</td>
        </tr>
        </tbody>
    </table>

    <ul>

        <li>
            <a href="{{ path('syndic_edit', { 'id': syndic.id }) }}">Éditert</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
